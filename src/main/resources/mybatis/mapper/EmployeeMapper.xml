<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fleta.employee.model.dao.EmployeeMapper">

    <select id="getAllEmployee" resultType="com.fleta.employee.model.dto.Employee">
        SELECT * FROM employee;
    </select>

    <select id="addEmployee">
        INSERT INTO employee (사번, 이름, 입사년월일, 국적, 생년월일, 성별, 우편번호, 주소, 상세주소, 휴대폰번호, 이메일, 부서, 직급, 직종)
        VALUES(#{id}, #{name}, #{joinDate}, #{country}, #{birthday}, #{gender}, #{zipCode}, #{address}, #{addressDetail}, #{phoneNumber}, #{email}, #{department}, #{rank}, #{occupation});
    </select>

</mapper>