<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fleta.employee.model.dao.EmployeeMapper">

    <select id="getAllEmployee" resultType="com.fleta.employee.model.dto.Employee">
        SELECT * FROM employee;
    </select>

    <insert id="createEmployee" parameterType="Employee">
        INSERT INTO employee (사번, 비밀번호, 사진, 이름, 입사년월일, 국적, 생년월일, 성별, 우편번호, 주소, 상세주소, 전화번호, 휴대폰번호, 이메일,
                    부서, 직급, 직종, 퇴사년월일, 급여이체은행, 계좌번호, 예금주, 비고, 등급)
        VALUES      (#{사번}, #{비밀번호}, #{사진}, #{이름}, #{입사년월일}, #{국적}, #{생년월일}, #{성별}, #{우편번호}, #{주소}, #{상세주소}, #{전화번호}, #{휴대폰번호}, #{이메일},
                    #{부서}, #{직급}, #{직종}, #{퇴사년월일}, #{급여이체은행}, #{계좌번호}, #{예금주}, #{비고}, #{등급});
    </insert>

    <delete id="deleteEmployee" parameterType="String">
        DELETE FROM employee
        WHERE 사번 = #{사번};
    </delete>

</mapper>